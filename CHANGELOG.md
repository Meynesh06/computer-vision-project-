# Workspace Changelog

Chronological lab notebook for this workspace: what changed, what failed, what was verified, and what should happen next.

## 2026-07-02 — leJEPA soundness research (`jepa-vs-traditional-cv-ml`)

**Objective**: check whether the leJEPA-based approach in `docs/superpowers/specs/2026-07-02-lejepa-tissue-classifier-design.md` is a sound choice for the NCT-CRC-HE-100K tissue classifier, versus traditional CV/ML and existing pathology foundation models. Framed as portfolio-project research supporting a job application (Aganitha, Computer Vision Engineer — Biomedical Imaging).

**Process**: Feynman `/deepresearch`-style workflow, adapted to Claude Code tooling. Plan → independent fable-subagent plan review (flagged model-tiering and one-sidedness issues, fixed before execution) → 6 parallel researcher subagents (haiku for search-heavy tasks, sonnet for theory/judgment-heavy tasks) → lead-synthesized draft → verifier subagent (sonnet, citation checks via arXiv/GitHub APIs) → adversarial reviewer subagent (opus) → fix pass → delivered.

**Verified**: `PASS WITH NOTES`. Delivered brief at `outputs/jepa-vs-traditional-cv-ml.md`, provenance at `outputs/jepa-vs-traditional-cv-ml.provenance.md`.

**Key finding**: NCT-CRC-HE-100K/CRC-VAL-HE-7K is already near-saturated by supervised/transfer learning (94.3-98.3% published), and SSL's classical label-efficiency advantage doesn't apply to a fully-labeled dataset. Existing open pathology foundation models (Virchow, GigaPath, UNI) offer a faster, more job-relevant path than pretraining leJEPA from scratch, though their specific accuracy figures on this dataset are reported, not independently confirmed (two behind paywalls). leJEPA's own counter-argument (in-domain SSL beating transfer on visually-specialized domains, per its Galaxy10 result) is real but untested on histology — treated as a live open question, not refuted.

**Verification caught a real issue**: a citation-verification pass found one claim (leJEPA underperforming MAE/DINOv3 on histopathology, sourced to a University of Kentucky project page) was not actually supported by its cited source when re-fetched directly — likely a fabrication/misread by a haiku-tier researcher subagent. Removed from the final brief rather than merely hedged. A subsequent adversarial review pass also caught the synthesized draft overstating verification status ("already verified" for numbers the brief's own notes called unconfirmed) and an overreaching verdict; both fixed and confirmed on disk before delivery.

**Next recommended step**: revisit `docs/superpowers/specs/2026-07-02-lejepa-tissue-classifier-design.md` in light of this research — decide whether to (a) pivot the project spec to fine-tuning an open pathology foundation model instead of leJEPA pretraining, given it's cheaper and more directly matches the target job description, or (b) keep leJEPA but reframe the project's stated goal explicitly as "demonstrating SSL engineering competence" rather than "maximizing classification accuracy," since the research does not support the latter framing on this specific dataset.

## 2026-07-03 — Cloud fine-tune on GCP: Phase 1 code fixes (`gcp-cloud-finetune`)

**Objective**: run the real 50-epoch Virchow LoRA fine-tune on GCP Compute Engine (pivoted from the Colab notebook — see `notes/2026-07-03-session-log.md` for the earlier brainstorming). Before any cloud spend, fix the correctness gaps that made the existing pipeline unsafe for a preemptible Spot VM (previously identified but deferred, per the session log's "code-fixes sub-project" split), then build GCP infra scripts, then empirically compare GPU tiers under a ~$10 cap, then run the full job under a $300 budget with a $40/job watchdog cap. Plan at `~/.claude/plans/cheerful-twirling-bentley.md` (4 phases: code fixes → GCP scripts → calibration → full run).

**Phase 1 complete — verified**: all 9 planned code-fix tasks landed and are covered by tests (`uv run pytest -q`: 60 passed, up from 43).

- **Checkpoint scope bug fixed**: `get_lora_state`/`load_lora_state` (`finetune.py`) now save only the LoRA adapter deltas + head via `peft.get_peft_model_state_dict`, not the full classifier `state_dict()` (which included the frozen 632M-param backbone). Confirmed on real Virchow weights: checkpoint file is 2.7MB, not multi-GB.
- **Resume-count bug fixed**: `train_lora` now takes `start_epoch`/`optimizer_state` and only trains `range(start_epoch, epochs)`; `run_cloud_train.main()` passes the checkpointed epoch through. Confirmed on real Virchow weights: trained 3 epochs fresh, then resumed with `epochs_lora: 5` and verified (via the test suite's spy, and manually) it only ran the remaining 2 epochs, not all 5 again.
- **`checkpoint_every_steps` now wired** via an `on_checkpoint` callback threaded through `train_lora`, instead of being dead config.
- **Embedding extraction is now resumable**: `extract_embeddings` periodically flushes a partial cache; a preemption mid-extraction (likely a multi-hour phase at full 100K scale) no longer loses all progress.
- **GCS checkpoint/results sync added** (`gcs_checkpoint_bucket` config key, off by default; fail-soft on `gsutil` errors so a network blip can't kill a training run).
- **Multi-GPU support added** via `torch.nn.DataParallel` (`num_gpus` config key) — chosen over DDP/torchrun as the right scope for a short GPU-tier calibration exercise, not a multi-node production setup.
- **`download_data.sh` now actually downloads** (real Zenodo URLs for record 1214456, verified reachable via `curl -I` before wiring in) instead of just printing the Zenodo page.
- **New `configs/calibration.yaml`** (2 epochs, small subset) for Phase 3's GPU-tier comparison; `gcs_checkpoint_bucket`/`num_gpus` added to `configs/lora_finetune.yaml`.
- **Docs fix pass** on `docs/superpowers/specs/2026-07-02-lejepa-tissue-classifier-design.md`: corrected the false "Apache 2.0, no access restrictions" claim (Virchow is HF-gated despite its Apache license) and the false "GigaPath is a drop-in fallback" claim (untested, also gated); added the real 2560-dim embedding fact; fixed the stale ~24GB dataset-size estimate (actual ~11GB); documented that checkpoint-scope/resume are now actually implemented, not just designed.
- **Real-weights verification** (not just mocked tests): ran `run_cloud_train.py` against real Virchow weights on the 180-image local subset — fresh 3-epoch run (probe/LoRA accuracy both 1.0) followed by a resume-to-5-epochs run, confirming the fixes work end-to-end, not just against fakes.

**Next recommended step**: Phase 2 — build the `scripts/gcp/` infra (preflight, budget alert, data upload, VM creation, deploy/run, on-VM cost-cap watchdog, local monitor, teardown), all behind a `DRY_RUN` preview convention, per the plan file. `gcloud` is installed locally but not yet authenticated — `gcloud auth login` + project selection is needed before Phase 2's preflight script can do anything real.

## 2026-07-03 (cont.) — GCP account setup + Phase 2 infra scripts (`gcp-cloud-finetune`)

**GCP account state**: authenticated as `meynesh0606@gmail.com`; active project set to `project-372251c6-08c2-4f37-907` (billing enabled). Enabled `compute.googleapis.com` and `billingbudgets.googleapis.com`.

**Blocker (still open)**: this project's global `GPUS_ALL_REGIONS` quota is 0 — no GPU VM can be created anywhere in it yet, regardless of per-region quota numbers. User is requesting an increase via Console > IAM & Admin > Quotas. `scripts/gcp/00_preflight.sh` checks this live and fails loudly until it's granted — verified still failing as of this writing.

**Currency issue found and fixed**: this billing account invoices in **INR**, not USD (India-based account). A first `gcloud billing budgets create --budget-amount=300USD` call failed with a generic 400 error — root cause was the currency mismatch, not bad flag syntax (confirmed by isolating each flag against the live API). User confirmed all budget figures ($300 total / $40 per-job / $10 calibration) are USD as intended. Fixed via a `USD_TO_INR_RATE` constant + `usd_to_inr()` helper in `_lib.sh` (rate ~95, checked via web search 2026-07-03) — only the Billing Budget object needs INR conversion (API requirement); the on-VM watchdog cost cap and all calibration cost projections stay in USD internally. A correct **₹28,500 (≈$300 USD)** budget with 50/90/100% email-alert thresholds is now live on the billing account, scoped to this project. (Two throwaway test budgets created while debugging the currency bug were deleted, none left behind.)

**Phase 2 complete — all `scripts/gcp/` infra built, dry-run-previewed, and (where possible without GPU quota) exercised for real**:

- `_lib.sh` — shared `DRY_RUN=1` preview wrapper, project/zone/bucket defaults, USD→INR conversion helper.
- `00_preflight.sh` — live-checks auth, billing, required APIs, **global + per-zone GPU quota** (the global check is what actually matters — confirmed catching the real blocker), local data, HF token, test suite. Verified working end-to-end (correctly fails today on the quota gate, passes everything else).
- `01_budget_alert.sh` — creates the (INR-converted) billing budget. Run for real; budget is live.
- `02_upload_data.sh` — creates the GCS bucket, uploads the full dataset zips (reuses already-downloaded local zips, never re-fetches from Zenodo) + builds/uploads a small calibration subset (60/class train, 20/class eval, sampled from the already-extracted CRC-VAL-HE-7K rather than extracting the 100K zip locally just for this). **Run for real**: bucket `gs://project-372251c6-08c2-4f37-907-tissue-classifier` now holds both dataset zips (11.63 GiB total, byte-for-byte verified against source) and the calibration subset.
- `03_create_vm.sh` — parametrized Spot VM creation. GPU-count → machine-type mapping (L4: 1→g2-standard-4, 2→g2-standard-24, 4→g2-standard-48, 8→g2-standard-96; A100: 1/2/4/8→a2-highgpu-Ng) was **verified live via `gcloud compute machine-types describe`** rather than assumed — confirmed g2-standard-32 has only 1 GPU despite 32 vCPUs, so a naive vCPU/4 formula would have been wrong, and confirmed "3x L4" is not a real GCE configuration (counts are exactly {1,2,4,8}). Requires an explicit `--spot-price-usd` flag with no default: third-party pricing sites disagreed by up to ~8x for the same SKU when checked, and the Cloud Billing Catalog API has no practical server-side filter for a single SKU lookup, so the honest choice is to require a fresh manual price check at VM-creation time rather than bake in a possibly-wrong constant. Dry-run verified the quota-refusal gate correctly blocks VM creation (no charge) given today's 0 global quota.
- `_remote_bootstrap.sh` / `04_deploy_and_run.sh` — tarball+scp code transfer (no git remote configured; `gcloud compute scp` itself has no `--exclude` support so a local tarball is built first, excluding `.venv`/`data/raw`/`outputs`/etc.), remote `uv sync` → test-suite sanity gate (fail fast before pulling data) → GCS data pull → HF token setup from instance metadata → config patch (`gcs_checkpoint_bucket`, `num_gpus` auto-detected via `nvidia-smi -L`) → detached watchdog + training launch (`nohup ... & disown`, survives SSH disconnect).
- `05_watchdog.sh` — on-VM cost-cap loop; deliberately uses `shutdown -h now` rather than `gcloud compute instances delete` on itself, avoiding the need to grant the VM's service account `compute.instances.delete` on itself (a broader IAM scope than warranted) — a stopped Spot VM already stops the dominant GPU-hour charge; `07_teardown.sh` cleans up the (much cheaper) leftover disk afterward.
- `06_monitor.sh` — local polling loop (GPU util, log tails, stall detection, and clear "VM may be preempted/self-terminated/torn down" reporting after repeated SSH failures rather than looping silently).
- `07_teardown.sh` — deletes the VM, leaves the GCS bucket intact by default (type-to-confirm required for `--delete-bucket`). Verified dry-run against a nonexistent VM name.

**Verified**: all 10 `scripts/gcp/*.sh` files pass `bash -n`; local test suite still 60/60 green throughout Phase 2 (no Python code touched, only new shell scripts + data upload).

**Next recommended step**: waiting on the GPU quota increase. Once granted, re-run `scripts/gcp/00_preflight.sh` to confirm, then proceed to Phase 3 (calibration): create a small L4 VM via `03_create_vm.sh` (with a freshly-checked `--spot-price-usd`), deploy the calibration config via `04_deploy_and_run.sh --data-mode calibration`, monitor via `06_monitor.sh`, record throughput/cost-projection results, teardown, and repeat for any other GPU configs the live quota check shows as viable -- before touching the full 50-epoch run.
